apiVersion: v1
kind: Service
metadata:
  name: book-catalog
  labels:
    app.kubernetes.io/name: book-catalog
spec:
  selector:
    app.kubernetes.io/name: book-catalog-dc
  ports:
    - name: http
      port: 8080
      targetPort: http
